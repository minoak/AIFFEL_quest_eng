"""Ablation eval: grounding ON vs OFF.  Owner: eval.

The demo's credibility argument: turn retrieve() off and show the
personas collapse into stereotypes. This is the Park et al. style
validation (component ablation), not future-accuracy.
"""


def run_ablation(policy: str, personas: list) -> dict:
    # TODO: run the graph with grounding enabled vs disabled, compare outputs.
    return {}
